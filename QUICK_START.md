# 🚀 دليل التخصيص السريع

هذا الملف يساعدك على تخصيص الموقع بسرعة لعملائك.

## ⚡ التخصيص في 3 خطوات

### 1️⃣ تغيير بيانات المتجر (دقيقة واحدة)

افتح ملف `data/store-config.json` وعدّل:

```json
{
  "storeName": "اسم المتجر الجديد",
  "whatsappNumber": "966501234567",
  "instagram": "https://instagram.com/yourstore",
  "facebook": "https://facebook.com/yourstore",
  "googleAnalyticsId": "G-XXXXXXXXXX"
}
```

**ملاحظات مهمة:**
- رقم واتساب: بدون `+` أو مسافات (مثال: `966501234567`)
- الروابط: يجب أن تبدأ بـ `https://`
- Google Analytics ID: اتركه فارغًا `""` إذا لم تكن تريد استخدام Analytics

---

### 2️⃣ إضافة/تعديل العطور (5 دقائق)

افتح ملف `data/perfumes.json`:

**لإضافة عطر جديد:**
```json
{
  "id": "23",
  "name": "اسم العطر",
  "description": "وصف قصير للعطر",
  "image": "/images/Perfum_img(23).png",
  "sizes": [
    { "size": "30ml", "price": 150 },
    { "size": "50ml", "price": 220 },
    { "size": "100ml", "price": 350 }
  ]
}
```

**لحذف عطر:**
- احذف الكائن (object) الخاص به من المصفوفة

**لتعديل عطر موجود:**
- غيّر القيم داخل الكائن

---

### 3️⃣ تغيير الصور (دقيقتان)

1. ضع صور العطور في مجلد `public/images/`
2. اسم الصورة يجب أن يطابق المسار في `perfumes.json`
   - مثال: إذا كان المسار `/images/Perfum_img(1).png`
   - يجب أن تكون الصورة: `Perfum_img(1).png`

**نصائح للصور:**
- الحجم الموصى به: 800x800 بكسل
- الصيغة: PNG أو JPG
- الحجم: أقل من 500KB للصورة الواحدة

---

## 📊 إضافة Google Analytics (اختياري)

**ما هو Google Analytics؟**
- أداة مجانية لتتبع زوار الموقع
- تعرف عدد الزوار، الصفحات الأكثر زيارة، المصدر (Instagram, Facebook, etc)
- مفيد جدًا لمعرفة أداء الموقع

**كيفية الحصول على Google Analytics ID:**

1. اذهب إلى [Google Analytics](https://analytics.google.com/)
2. سجّل حساب (مجاني)
3. أنشئ Property جديد
4. انسخ الـ Measurement ID (يبدأ بـ `G-`)
5. أضفه في `data/store-config.json`:
   ```json
   "googleAnalyticsId": "G-XXXXXXXXXX"
   ```

**ملاحظات:**
- إذا تركت `googleAnalyticsId` فارغًا `""`، لن يتم تحميل Google Analytics
- الموقع يعمل بشكل طبيعي بدون Analytics
- Analytics يبدأ التتبع تلقائيًا بعد إضافة ID

**الأحداث التي يتم تتبعها تلقائيًا:**
- ✅ الضغط على زر واتساب (من Hero, Modal, Footer)
- ✅ فتح Modal للعطر
- ✅ اختيار حجم العطر
- ✅ الضغط على "اطلب عبر واتساب"
- ✅ الضغط على روابط السوشيال ميديا

---

## 🎨 تغيير الألوان (اختياري)

افتح ملف `app/globals.css` وابحث عن:

```css
:root {
  --color-primary: #1A2332;
  --color-secondary: #C9A961;
  /* ... */
}
```

غيّر القيم حسب ما تريد.

---

## 📱 اختبار التغييرات

بعد أي تعديل:

```bash
npm run dev
```

افتح المتصفح على: `http://localhost:3005`

---

## 🚀 النشر (Deployment)

### Vercel (موصى به - مجاني):

1. ارفع المشروع على GitHub
2. اذهب إلى [vercel.com](https://vercel.com)
3. اضغط "New Project"
4. اختر المشروع من GitHub
5. اضغط "Deploy"

**ملاحظة:** Vercel يكتشف Next.js تلقائيًا ولا يحتاج إعدادات إضافية.

---

## ❓ مشاكل شائعة

**المشكلة:** الصور لا تظهر
- **الحل:** تأكد أن الصور موجودة في `public/images/` وأن المسار في `perfumes.json` صحيح

**المشكلة:** واتساب لا يفتح
- **الحل:** تأكد أن رقم واتساب في `store-config.json` صحيح (بدون + أو مسافات)

**المشكلة:** التصميم يبدو غريبًا
- **الحل:** تأكد أنك شغلت `npm run dev` بعد التعديلات

---

## 📞 الدعم

إذا واجهت مشكلة، تأكد من:
1. قراءة `README.md` بالكامل
2. التحقق من أن جميع الملفات موجودة
3. التأكد من تثبيت المكتبات: `npm install`

---

**⏱️ الوقت المتوقع للتخصيص الكامل: 10-15 دقيقة**
