# 🧪 دليل اختبار Open Graph Image

هذا الدليل يوضح كيفية اختبار أن الصورة تظهر بشكل صحيح عند مشاركة رابط الموقع.

## 📋 طرق الاختبار

### 1️⃣ اختبار محلي (قبل النشر)

#### أ) فحص Source Code
1. شغّل المشروع:
   ```bash
   npm run dev
   ```

2. افتح المتصفح على: `http://localhost:3005`

3. اضغط `Ctrl + U` (أو `Cmd + U` على Mac) لعرض Source Code

4. ابحث عن `og:image` - يجب أن تجد:
   ```html
   <meta property="og:image" content="https://yoursite.com/images/Perfum_img(21).png" />
   ```

#### ب) فحص Meta Tags في DevTools
1. افتح `F12` (Developer Tools)
2. اذهب إلى تبويب **Elements** (أو **Elements**)
3. ابحث في `<head>` عن جميع meta tags التي تبدأ بـ `og:`

#### ج) استخدام أدوات Online (يحتاج URL عام)
- **Open Graph Preview**: https://www.opengraph.xyz/
- **Meta Tags Checker**: https://metatags.io/

---

### 2️⃣ اختبار بعد النشر (الأهم!)

#### أ) Facebook Sharing Debugger
1. اذهب إلى: https://developers.facebook.com/tools/debug/
2. أدخل رابط موقعك (مثلاً: `https://yourdomain.com`)
3. اضغط **Debug**
4. اضغط **Scrape Again** لمسح الـ Cache
5. تحقق من:
   - ✅ **og:image** يظهر الصورة
   - ✅ **og:title** يظهر العنوان
   - ✅ **og:description** يظهر الوصف

#### ب) WhatsApp (الطريقة الأسهل)
1. **نشر الموقع أولاً** على خادم (Vercel, Netlify, etc.)
2. افتح WhatsApp على هاتفك
3. أرسل رابط الموقع **لنفسك** (Saved Messages)
4. تحقق من:
   - ✅ الصورة تظهر مع الرابط
   - ✅ العنوان والوصف يظهران

**ملاحظة مهمة**: WhatsApp يستخدم Cache - إذا لم تظهر الصورة:
- انتظر 5-10 دقائق
- أو استخدم Facebook Debugger أولاً لمسح Cache

#### ج) Twitter Card Validator
1. اذهب إلى: https://cards-dev.twitter.com/validator
2. أدخل رابط موقعك
3. اضغط **Preview card**
4. تحقق من ظهور الصورة

#### د) LinkedIn Post Inspector
1. اذهب إلى: https://www.linkedin.com/post-inspector/
2. أدخل رابط موقعك
3. اضغط **Inspect**
4. تحقق من Preview

---

### 3️⃣ اختبار سريع باستخدام curl

```bash
# استبدل yourdomain.com برابط موقعك
curl -I https://yourdomain.com/images/Perfum_img(21).png
```

يجب أن ترى:
- `HTTP/1.1 200 OK`
- `Content-Type: image/png`

---

## ✅ Checklist قبل الاختبار

- [ ] تم تعيين `NEXT_PUBLIC_SITE_URL` في `.env.local`
- [ ] تم نشر الموقع على خادم (Vercel/Netlify/etc.)
- [ ] الصورة موجودة في `public/images/Perfum_img(21).png`
- [ ] تم بناء المشروع بنجاح (`npm run build`)

---

## 🔧 حل المشاكل الشائعة

### المشكلة: الصورة لا تظهر في WhatsApp
**الحل**:
1. تأكد من أن `NEXT_PUBLIC_SITE_URL` مضبوط بشكل صحيح
2. استخدم Facebook Debugger لمسح Cache
3. انتظر 5-10 دقائق بعد النشر
4. تأكد من أن الصورة URL مطلق (يبدأ بـ `https://`)

### المشكلة: الصورة لا تظهر في Facebook
**الحل**:
1. استخدم Facebook Debugger
2. اضغط **Scrape Again** عدة مرات
3. تأكد من أن الصورة:
   - حجمها أكبر من 200x200px
   - نوعها PNG أو JPG
   - URL مطلق وليس نسبي

### المشكلة: الصورة تظهر لكن بحجم خاطئ
**الحل**:
- الأبعاد المثالية: 1200x630px (للـ Facebook)
- أو 1200x1200px (مربع - يعمل على WhatsApp)
- يمكن تعديل الأبعاد في `app/layout.tsx`

---

## 📝 ملاحظات مهمة

1. **URL يجب أن يكون مطلق**: 
   - ✅ صحيح: `https://yourdomain.com/images/Perfum_img(21).png`
   - ❌ خطأ: `/images/Perfum_img(21).png`

2. **Cache**: 
   - Facebook و WhatsApp يستخدمان Cache
   - قد تحتاج لمسح Cache عدة مرات

3. **HTTPS مطلوب**:
   - يجب أن يكون الموقع على HTTPS
   - الصورة يجب أن تكون قابلة للوصول عبر HTTPS

---

## 🚀 خطوات سريعة للاختبار

```bash
# 1. تأكد من وجود .env.local
echo "NEXT_PUBLIC_SITE_URL=https://yourdomain.com" > .env.local

# 2. بناء المشروع
npm run build

# 3. نشر على Vercel (مثلاً)
vercel --prod

# 4. اختبار على Facebook Debugger
# https://developers.facebook.com/tools/debug/

# 5. اختبار على WhatsApp
# أرسل الرابط لنفسك
```

---

## 📞 مساعدة إضافية

إذا استمرت المشكلة:
1. تحقق من Console في المتصفح (F12)
2. تحقق من Network tab - هل الصورة تحمّل؟
3. استخدم Facebook Debugger لرؤية الأخطاء
4. تأكد من أن الصورة موجودة فعلاً في `public/images/`

---

**تم إنشاء هذا الدليل في:** $(date)
