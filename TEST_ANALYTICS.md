# 🧪 دليل اختبار Google Analytics

## ✅ اختبار سريع (5 دقائق)

### الخطوة 1: تحقق من التحميل
1. افتح الموقع في المتصفح
2. اضغط `F12` لفتح Developer Tools
3. اذهب إلى تبويب **Console**
4. اكتب: `window.gtag`
5. **النتيجة المتوقعة:** يجب أن ترى `function gtag() { ... }`

### الخطوة 2: اختبار Real-Time
1. افتح [Google Analytics](https://analytics.google.com)
2. اذهب إلى **Reports** → **Real-time**
3. افتح موقعك في نافذة أخرى
4. **النتيجة المتوقعة:** يجب أن ترى "1 user" في Real-time

### الخطوة 3: اختبار الأحداث
1. في Console، اكتب:
   ```javascript
   window.dataLayer = window.dataLayer || [];
   const originalPush = window.dataLayer.push;
   window.dataLayer.push = function(...args) {
     console.log('📊 GA Event:', args);
     return originalPush.apply(window.dataLayer, args);
   };
   ```
2. اضغط على زر واتساب في Header
3. **النتيجة المتوقعة:** يجب أن ترى في Console:
   ```
   📊 GA Event: ['event', 'whatsapp_click', {location: 'hero', ...}]
   ```

---

## 🔍 اختبار شامل (15 دقيقة)

### 1. اختبار جميع الأحداث:

| الإجراء | الحدث المتوقع | كيفية التحقق |
|---------|---------------|---------------|
| الضغط على زر واتساب (Header) | `whatsapp_click` | Console أو Real-time Events |
| الضغط على بطاقة عطر | `product_card_click` | Console |
| فتح Modal | `product_view` | Console |
| اختيار حجم | `size_selection` | Console |
| الضغط "اطلب عبر واتساب" | `order_click` | Console |
| الضغط على واتساب (Footer) | `whatsapp_click` | Console |
| الضغط على Instagram | `social_click` | Console |

### 2. التحقق من Real-Time Events:
1. في Google Analytics → Real-time
2. اضغط على **Events** في القائمة الجانبية
3. نفّذ الإجراءات أعلاه
4. **النتيجة المتوقعة:** يجب أن ترى الأحداث تظهر في Real-time

### 3. التحقق من Page Views:
1. في Google Analytics → Real-time
2. اضغط على **Overview**
3. **النتيجة المتوقعة:** يجب أن ترى:
   - Active users: 1
   - Top pages: اسم صفحتك
   - Top locations: موقعك الجغرافي

---

## 🐛 حل المشاكل الشائعة

### المشكلة: `window.gtag` هو `undefined`
**الحل:**
1. تحقق من أن `googleAnalyticsId` موجود في `store-config.json`
2. تأكد من أن ID يبدأ بـ `G-`
3. أعد تحميل الصفحة (`Ctrl+F5`)

### المشكلة: لا تظهر الأحداث في Real-time
**الحل:**
1. انتظر 10-30 ثانية (Google Analytics يحتاج وقت)
2. تأكد من أنك في Real-time وليس في Reports العادية
3. تحقق من Console للأخطاء

### المشكلة: الأحداث لا تظهر في Console
**الحل:**
1. تأكد من أنك نسخت الكود بشكل صحيح
2. تأكد من أنك في Console وليس Network
3. أعد تحميل الصفحة بعد إضافة الكود

---

## 📊 الأحداث المتوقعة

بعد تنفيذ جميع الإجراءات، يجب أن ترى هذه الأحداث:

```
📊 GA Event: ['event', 'whatsapp_click', {location: 'hero', ...}]
📊 GA Event: ['event', 'product_card_click', {product_id: '1', ...}]
📊 GA Event: ['event', 'product_view', {product_id: '1', ...}]
📊 GA Event: ['event', 'size_selection', {product_id: '1', size: '30ml', ...}]
📊 GA Event: ['event', 'order_click', {product_id: '1', price: 150, ...}]
📊 GA Event: ['event', 'whatsapp_click', {location: 'modal', ...}]
📊 GA Event: ['event', 'social_click', {platform: 'instagram', ...}]
```

---

## ✅ قائمة التحقق النهائية

- [ ] `window.gtag` موجود ويعمل
- [ ] Real-time يظهر "1 user" عند فتح الموقع
- [ ] جميع الأحداث تظهر في Console
- [ ] جميع الأحداث تظهر في Real-time Events
- [ ] لا توجد أخطاء في Console

---

## 🎯 النتيجة النهائية

إذا نجحت جميع الاختبارات أعلاه، **Google Analytics يعمل بشكل صحيح!** ✅
